/** Shape used in React state and NoteCard/OpenNote (flat markdown string, string id). */
export interface Note {
   id: string;
   title: string;
   content: string;
   created_at: string;
   updated_at: string;
}

